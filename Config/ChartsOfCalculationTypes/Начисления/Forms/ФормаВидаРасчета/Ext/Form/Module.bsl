

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	Если Объект.Предопределенный Тогда
		Элементы.ФормулаРасчета.Доступность = Ложь;
	Иначе
		Элементы.ФормулаРасчета.Доступность = Истина;
	КонецЕсли;
КонецПроцедуры

